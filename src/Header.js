import React from "react";

function Header() {
    return (
        <header>
            <h1>Site where you can come & watch shell based videos!</h1>
        </header>
    );
}

export default Header;